# Chatbot-backend
A modern, production-grade FastAPI backend for context-aware AI chat applications. Features secure JWT + server-side session management, persistent multi-turn conversation memory stored in PostgreSQL, full chat history persistence, and clean separation of authentication, business logic, and LLM integration.
